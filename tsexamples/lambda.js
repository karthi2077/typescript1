var addNum = function (n1, n2) { console.log(n1 + n2); };
var multiNum = function (n1, n2) { console.log(n1 * n2); };
var dividNum = function (n1, n2) { console.log(n1 / n2); };
addNum(10, 2);
multiNum(10, 2);
dividNum(10, 2);
